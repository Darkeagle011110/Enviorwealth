--extra-index-url https://download.pytorch.org/whl/cpu

# ─── System / Core ───────────────────────────────────────────────────────────
numpy>=2.1.0

# ─── Web Framework ───────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9       # file uploads
aiofiles==23.2.1              # async static file serving

# ─── Database ────────────────────────────────────────────────────────────────
motor>=3.6.0                  # async MongoDB driver (built on PyMongo)
pymongo>=4.9.0                # sync MongoDB driver (motor dependency + direct use)
qdrant-client>=1.12.0         # Qdrant vector DB client (Cloud + self-hosted)

# ─── Validation ──────────────────────────────────────────────────────────────
pydantic[email]==2.8.2
pydantic-settings==2.4.0

# ─── LLM Providers ───────────────────────────────────────────────────────────
anthropic==0.34.2             # Claude
openai==1.45.0                # OpenAI GPT
groq==0.10.0                  # Groq (Llama, Mixtral)
google-generativeai==0.7.2    # Google Gemini

# ─── RAG / Embeddings ────────────────────────────────────────────────────────
langchain>=0.3.1
langchain-community>=0.3.1
langchain-openai>=0.2.1
langgraph==0.2.22
sentence-transformers>=2.2.0  # local embedding fallback
rank-bm25==0.2.2              # BM25 sparse retrieval

# ─── Document Parsing ────────────────────────────────────────────────────────
pypdf==4.3.1
pdfplumber==0.11.3
python-docx==1.1.2

# ─── Config / Utilities ──────────────────────────────────────────────────────
pyyaml==6.0.2
python-dotenv==1.0.1
httpx==0.27.2                 # async HTTP for health checks
tenacity<9.0.0               # retry logic for LLM fallback
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==43.0.0

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest==8.3.2
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2                 # async test client
